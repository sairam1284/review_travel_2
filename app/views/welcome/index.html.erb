<h1>Welcome to the product review page</h1>

<% if user_signed_in? %>
  <h3> Welcome <u><%= current_user.name %>. Thanks for signing in!</u></h3>
<% else %>
  <h3>Sign in if you would like to write a review </h3>
<% end %>

<ol>
  <% @users.each do |user| %>
  <li>Name: <%= user.name%>,  Email: <%= user.email %></li>
  <%end%>
</ol>
