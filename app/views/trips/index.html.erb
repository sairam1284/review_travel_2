<h1>1. Welcome to the Travel Trips Home Page</h1>

<% if user_signed_in? %>
  <h3> 2. Welcome <u><%= current_user.name %>. Thanks for signing in!</u></h3>
<% else %>
  <h3>2. Sign in if you would like to write a review </h3>
<% end %>

<h2> 3. List of trips </h2>
<table border = "1">
  <thead>
    <tr>
      <th>Title</th>
      <th>Location</th>
      <th>Duration</th>
      <th>Price</th>
      <th>Rating</th>
    </tr>
  </thead>

  <tbody>
    <% @trips.each do |trip| %>
      <tr>
        <td><strong><%= link_to trip.title, trip%> </strong></td>
        <td>  <%= trip.location %></td>
        <td>  <%= trip.length %></td>
        <td>  <%= trip.price %></td>
        <td> N/A </td>
      </td>
    <%end%>
  </tbody>
</table>

<h4><%= link_to "Add New Trip", new_trip_path %></h4>

<h3> 4. List of users </h3>
<ol type="a">
  <% @users.each do |user| %>
  <li>Name: <%= user.name%>,  Email: <%= user.email %></li>
  <%end%>
</ol>
